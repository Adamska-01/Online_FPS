using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class matTest : MonoBehaviour
{
    public MaterialController controller = null;    
    void Start()
    {
        controller.OnStart();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
